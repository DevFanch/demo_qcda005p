<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="#">Demo</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
                <a class="nav-link active" title="Accueil" href="{{ path('app_home')}}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" title="Test" href="{{ path('course_list')}}">Cours</a>
				</li>
                {% if app.user %}
				<li class="nav-item">
					<a class="nav-link" title="Test" href="{{ path('course_create')}}">Créé</a>
				</li>
                    {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item">
                        <a class="nav-link" title="Test" href="{{ path('admin_dashboard')}}">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" title="Test" href="{{ path('admin_course')}}">Gestion des cours</a>
                    </li>
                    {% endif %}
                <li class="nav-item">
                    <a class="nav-link" title="Test" href="{{ path('app_logout')}}">Déconnexion</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" title="Test" href="{{ path('app_register')}}">Inscription</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" title="Test" href="{{ path('app_login')}}">Connexion</a>
                </li>
                {% endif %}
			</ul>
		</div>
	</div>
</nav>
